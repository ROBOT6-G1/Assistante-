rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for the isolated development/preview database to support 
    // both authenticated client operations and unauthenticated server-side fallback
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
